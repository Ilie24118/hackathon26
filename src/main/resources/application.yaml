spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

  messages:
    basename: message

  mvc:
    hiddenmethod:
      filter:
        enabled: true

server:
  port: ${PORT:8080}
